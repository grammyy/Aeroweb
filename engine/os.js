window.onload=function(){

}
var API = {
    //spash:function(data,alignment,sheet,delays){
    //    objects=[]
    //    time=Date.now();objects.append(alignment[0].insertAdjacentHTML("beforeEnd","<div id='"+time+"'></div>"))
    //    switch(alignment[1]){
    //        case 'center':
    //            for(var index=0;index<sheet.length;index++){
    //                objects.append(objects[0].insertAdjacentHTML("beforeEnd",sheet[index]));
    //                setTimeout(function(){objects[index+1].style.opacity="0%";setTimeout(function(){objects[index+1]},delays[index].remove())},delays[index])
    //            }
    //            break
    //    }
    //}
}